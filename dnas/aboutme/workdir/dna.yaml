manifest_version: "0"
name: aboutme
integrity:
  network_seed: null
  # The base cell is for development only, and must say so: a DNA with no
  # properties is now closed to everybody rather than open to everybody.
  properties:
    open_for_development: true
  zomes:
    - name: aboutme_integrity
      path: "../../../target/wasm32-unknown-unknown/release/aboutme_integrity.wasm"
coordinator:
  zomes:
    - name: aboutme
      path: "../../../target/wasm32-unknown-unknown/release/aboutme.wasm"
      dependencies:
        - name: aboutme_integrity
